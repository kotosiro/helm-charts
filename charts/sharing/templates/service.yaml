apiVersion: v1
kind: Service
metadata:
  name: {{ include "sharing.fullname" . }}
  labels:
    {{- include "sharing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sharing.selectorLabels" . | nindent 4 }}
